PHP基础知识
	cookie和session(会话控制原理。)										√
	ob																	√
	$_SERVER															
	正则表达式(邮箱，网址，标签匹配，正则函数)							√
	HTTP协议															√
	curl																(有待补充)
	PHP运行模式															√		
	fsocket
	xml																	√
	设计模式															
	PHP时间函数，日期计算函数				http://www.cnblogs.com/baochuan/archive/2012/08/17/2643716.html
	文件系统，记录日志、目录、文件的遍历、上传、多方法得到文件扩展名、文件引用方式、引用函数区别（面试必出）
	PHP模板引擎，常用模板引擎特点，MVC好与不好的地方
	PHP安全处理，过滤函数
	PHP字符串的处理，包括转义（安全）、编码、截取、定位、与数组间的转换、处理函数等。（面试必出）
	Socket编程，各种协议，head头，curl参数含义
	Apache配置文件，PHP配置文件，各个含义字段的含义		
	网络各种攻击的名词含义（SQL攻击、XSS、CSRF、DDos），防止措施。		(有待补充)
	url的处理函数，得到url指定的部分									(有待补充)

mysql
	mysql基础
	mysql索引基础
	mysql优化
	mysql处理大数据
	mysql常见面试题
	创建表时索引的应用

PHP框架基础
	代码重用：定义包、类、函数的放置和加载规则，建议直接整合Composer及其AutoLoad特性。
	请求的分发管理：这个就是路由，Rest风的框架喜欢Rewrite，简单的一点的框架主要通过参数来定位模块和方法所在。
	配置文件管理：加载和动态加载配置数据
	错误和异常管理：异常捕捉、错误日志记录以及错误码规范。
	LayOut和模板引擎：如何规划页面布局、widget如何重用、ajax页面如何结合、过期session如何重定向；数据和模板怎么渲染成HTML，是否压缩和设置过期头。
	数据库：如何融入控制器；支持什么样的driver；考虑主从分离的扩展性；以及是否使用ORM。
	一般框架 单一入口->路由->分发->渲染，加一些扩展性，给路由，分发和渲染加上接口或者抽象类，再方便点加上composer,再好维护点加模块，然后改BUG。。
	一般框架 单一入口->路由->分发->渲染，加一些扩展性，给路由，分发和渲染加上接口或者抽象类，再方便点加上composer,再好维护点加模块，然后改BUG。。

数据结构
	树

面试题目(知乎上的php面试题做一遍)

面试会提到的问题

github

项目
	日志系统
		登录
		错误异常
		正则
		防sql注入（安全问题）
	IP系统
	自己的框架
	上传